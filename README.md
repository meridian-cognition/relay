# context — retired

This channel is retired (D-AZ, 2026-09-19). It carried the project's context as
an encrypted snapshot until `2026-09-17a`.

Project context now lives in the repository at `context/` and travels in the
`repo` channel of this relay. Read that instead: the procedure is in
`skills/content-acquisition/references/relay.md` on the main branch of
meridian-cognition/content-acquisition.

MIRROR.json here is a signpost. There is no ciphertext and no key: the key at
`context-mirror/latest-key.json` on the store was removed when this channel was
retired.
